{
    "version": 2,
    "builds": [
      {
        "src": "alpine/wsgi.py",
        "use": "@vercel/python",
        "config": { 
            "runtime": "python3.12",
            "installCommand": "poetry install --no-dev && poetry run setup_prod"
        }
      },
      {
        "src": "static/**/*",
        "use": "@vercel/static"
      }
    ],
    "routes": [
      {
        "src": "/static/(.*)",
        "dest": "/static/$1"
      },
      {
        "src": "/(.*)",
        "dest": "alpine/wsgi.py"
      }
    ],
    "build": {
        "env": {
            "START_COMMAND": "python -m poetry run prod"
        },
        "commands":"poetry run setup_prod"
    }
  }
  